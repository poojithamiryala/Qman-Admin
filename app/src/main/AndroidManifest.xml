<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.poojithamiryala.admin_qma">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main_1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity
            android:name=".Admin_Login"
            android:label="@string/title_activity_admin__login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Update" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Step_add1" />
        <activity android:name=".StartQueue" />
        <activity android:name=".AddServices" />
        <activity android:name=".AddContact" />
        <activity android:name=".AddOrganization" />
        <activity android:name=".inputDialog" />
        <activity android:name=".Fragment11"></activity>
    </application>

</manifest>